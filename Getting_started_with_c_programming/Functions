/*Functions
    functions is a group of statements that performs a specific task
    functions can be used to divide our program into small blocks of codes
    use descriptive names for functions
    */
    
//Function parameters//
/* EXAMPLE 1
void calculateSquare(int number) {
    int square = number * number;
    printf("Square of %d is %d\n", number, square);
}

int main() {
calculateSquare(7);



    return 0;
}*/


/* EXAMPLE 2
void addNumbers(int number1, int number2) {
    int sum = number1 + number2;
    printf("sum of %d and %d is %d", number1, number2, sum);
}

int main() {
    addNumbers(12, 13);
    
    return 0;
}*/

//EXAMPLE 3//
#include <stdio.h>
int addNumbers(int number1, int number2) {
    int sum = number1 + number2;
    return sum;
}

int main() {
    
    int result = addNumbers(12, 13);
    printf("Result = %d", result);
    
    
    return 0;
}
